# 更新日志

历史版本的说明见 [GitHub Releases](https://github.com/my-name-is-alan/hack-guangya/releases)。

## 未发布

## v0.1.44 - 2026-08-19

- 修复 Docker Web 在 Chrome / Edge / Firefox 里新增整理监控时“整理方式 / 媒体类型 / 目标冲突”点不开选项（#3）：这三个短枚举改为按钮组，不再依赖会被抽屉 overflow/transform 裁掉的 Select 弹层；全局 Select 弹层改为挂到 `document.body` 并提高 z-index，其它抽屉里的下拉同样生效。
- 虚拟库 STRM 同步在删除过期文件后会自底向上收回变空的电影/剧集文件夹（#4）：例如 `电影/阿凡达/阿凡达.strm` 被删后，`阿凡达` 空目录一并删除；仍有 STRM 或元数据的目录、虚拟库根目录和用户自放的文件不受影响。
- 云端接口限流（业务码 120，提示“缺少clientId”）改为在两端 API 层统一退避重试：被限流的请求未被云端执行，读写接口都会自动指数退避重发（最多 6 次），操作过快不再把“缺少clientId”直接报给界面。
- 整理进度全局可见：顶栏新增“整理 N”指示器（识别中 + 整理中），点击直达整理页。
- 批量“重新识别”升级为“重新识别并整理”：后台识别成功后自动执行落位，不再停在“待执行”。
- 重新归档会先清理上一次的落位产物（旧视频/字幕/元数据，单集纠错不误删剧集共享海报），并自底向上删除变空目录。
- 执行整理时源目录出现新文件不再报错卡在待执行：自动按最新内容重新识别并执行。整理从不删除源侧父目录。

## v0.1.43 - 2026-08-13

- 修复大文件夹上传扫描漏文件：本地扫描会整棵跳过目录联接/符号链接（资源库常用 `mklink /J` 把部分内容挂到另一块盘），`read_dir` 失败或超长路径也会被静默丢掉，列表里的文件都上传成功但体积对不上。现在会跟随目录链接（带循环检测）、用 Windows `\\?\` 长路径读取，并把跳过的路径弹出来。
- 新增 Telegram Bot 通知与交互渠道（设置 → Telegram，桌面端与 Docker/Web 端一致）：支持 Bot API（直接 Bot Token，可选自建反代地址）与 MTProto（api_id/api_hash + Bot Token 直连）两种接入模式；推送整理入库、识别失败（附“重新识别 / 重新整理 / 填写 TMDB ID”按钮，支持 `re <任务ID> tmdbid=… [tv|movie] [s=1]` 命令）、光鸭登录失效（可直接在 Telegram 中扫码重登）；提供 `/status`、`/jobs`、`/logs`（最新运行日志）、`/update`（桌面端支持一键安装更新）、`/login` 命令菜单。
- 新增 Emby Webhook 接收端点（带密钥校验）：Emby 后台把 Webhook 指向本服务即可推送入库、播放开始/停止、用户登录事件到 Telegram；Docker/Web 在管理端口 `/webhooks/emby` 与网关端口 `/guangya/webhooks/emby`，桌面端在网关端口。
- 新增运行日志环形缓冲：Web 端提供 `GET /api/logs`，桌面端提供同名命令，Telegram `/logs` 可取最近 50 条。
- 整理任务支持批量重新识别：表格勾选多个任务一键重跑识别（沿用各自已保存的人工修正），识别失败的任务也提供“人工确认”入口。
- 修复点“重新识别”时弹出 cmd 黑窗：Windows 下 ffprobe 媒体探测子进程未隐藏控制台窗口。
- 人工确认对话框新增“高级选项”：集偏移（识别集号统一平移，可为负）与临时识别词（仅对该任务生效、优先于全局规则），两者随任务保存并在后续重识别时沿用。
- 顶部工具菜单新增“识别测试工具”：粘贴多个文件名即可查看分词解析结果（中英文名/年份/季集/内嵌 ID/技术信息），支持临时识别词调试与可选的 TMDB 匹配预览。

## v0.1.42 - 2026-08-13

### 界面优化

- 挂载设置页重排：状态条不再塞大段说明，网关 / STRM 直链 / Emby 上游改为紧凑的可复制端点行，虚拟库设置改为两列表单，Emby 使用说明折叠为可展开的条目列表，长段落全部精简。
- 任务列表页补齐筛选：整理任务（状态 + 关键字）、云添加（状态分段 + 关键字）、分享（有效/失效 + 关键字）、传输（进行中/已结束）、回收站与云端最近（本页关键字）。
- 表格操作列统一固定在右侧（整理任务、云添加、回收站、云端最近、分享），配合横向滚动不再被挤出视野；所有表格滚动条统一为细样式。

### 整理修复

- 修复批量/整理重命名大面积失败：云端 rename 接口有并发风控，过快调用会返回业务码 120（msg“缺少clientId”，文案有误导，实为限流）。整理器改名改为串行执行，且所有重命名调用（整理、批量重命名、离线改名恢复）遇 120 自动退避重试，实测并发场景下全部成功。
- 识别文件名中内嵌的 TMDB ID 并直接采用：支持 `{tmdb-123}`、`{tmdbid-123}`、`[tmdb=123]`、`-Tmdb123` 等写法（HDHive/Emby 风格）；ID 不带类型时电影/剧集各查一遍，按标题与年份消歧，避免同号错认。
- 修复制作组误判：`WEB-DL.AAC` 结尾会被当成制作组“DL.AAC”渲染进新文件名；技术词开头的尾缀不再视为制作组。
- 修复重命名模板渲染残留：空字段产生的 `WEB-DL..59fps` 双点、空制作组产生的 `AAC-.mkv` 悬空连字符现在会被清理（桌面端此前缺少双点折叠）。

### 识别引擎重写（对齐 MoviePilot v2 分词 + HDHive TMDB 优先策略）

- 文件名解析改为 MoviePilot v2 同款分词状态机：按分隔符拆 token 流逐个判定，中文名与英文名分开累计（`凡人修仙传.The.Immortal.Ascension.2020` 拆出两个标题分别搜索），自动剥离【字幕组】【X月新番】等前缀括号，支持中文数字季集（第三季/第十二集/全39集/01-26Fin 完结区间）、罗马数字、Part/CD、动漫“- 05”与【01】集号、纯数字标题（如《300》）与年份区分等。
- TMDB 匹配重写为多轮回退链：中文名 → 英文名逐个搜索；剧集=带年份搜 → 去年份重试，电影=电影 → 剧集 → /search/multi 兜底；命中 TMDB 标题/原始标题精确相等（忽略大小写与符号，年份 ±1）直接采用；第二轮拉取 TMDB 别名/多语言译名精确匹配；有年份时信任 TMDB 相关度排序取 ±1 年窗口内首个结果（HDHive 语义）；以上都未命中才回到本地相似度评分门槛与人工复核，人工选择链路不变。
- 匹配失败率显著下降的同时保留保守项：分词回退搜索、相似度阈值、歧义候选人工确认均保留原设置项。

### 整理洗版（同内容以旧换新）

- 目标冲突策略新增“洗版（替换旧版本）”：整理时在目标目录内识别同一内容的旧版本（电影按同 part、剧集按同季同集），按“分辨率 → 动态范围 → 制作组 → 文件大小”的可配置优先级比较，新版本更优时把旧视频连同其字幕/NFO 等伴随文件移入回收站后落位；现有版本更优或完全持平则跳过本次转移（字幕等跟随主视频结论）。
- 预览会明确展示洗版结论：替换项列出将被清理的旧文件清单与胜出维度，压制项说明现有版本在哪个维度更优；汇总新增“洗版替换/压制跳过”计数。
- “识别设置 → 洗版策略”可调整比较维度顺序与制作组优先级名单（每行一个，靠前优先，`#` 开头为注释）；选择洗版策略需确认分享失效风险（旧文件删除会使其分享失效）。

### 整理完成自动刷新 Emby

- 整理任务成功后自动匹配输出目录所在的 STRM 虚拟库（按目录 ID 或云端路径前缀）并触发同步，新媒体无需等定时刷新即可生成 STRM。
- 虚拟库同步现在收集变更清单（新增/修改/删除），配置 Emby API Key 后同步完成自动调用 `/Library/Media/Updated` 通知 Emby 按路径刷新；虚拟库映射可填“Emby 内部路径”（容器路径映射不一致时用于换算通知路径）。
- 同步进行中再次触发会合并排队，不再并发重复扫描。

## v0.1.41 - 2026-08-13

- 新增 Emby 兼容网关：客户端把网关地址（桌面端与 STRM 直链服务共用 18096，Docker 独立发布 18096 端口）当作 Emby 服务器使用，浏览、搜索、图片、WebSocket 全部转发到 Emby 原始服务；命中本服务签名 STRM 直链媒体源的原画播放请求（stream/original/Items File）直接 302 到云盘 CDN，播放数据不经过 Emby 服务器和本机。HLS 转码等请求仍交给 Emby。
- 网关按 PlaybackInfo 的媒体源 Path 识别签名直链并本地校验 HMAC，不再依赖清单扫描；直连 Emby 原生端口（8096）的播放方式继续可用。
- 虚拟库设置恢复“Emby 原始地址”，Docker 恢复 `GUANGYA_EMBY_UPSTREAM` 与 18096 端口发布（默认仅宿主机回环，可用 `GUANGYA_EMBY_GATEWAY_BIND` 放开）。

## v0.1.40 - 2026-08-13

- 桌面端 STRM 直链服务支持非本机访问：STRM 直链地址配置为非回环地址（Emby 在 Docker 容器或其他设备上）时自动监听所有网卡，保存设置后立即重新绑定，无需重启应用；留空或本机地址时保持仅监听 127.0.0.1。端点仍要求 HMAC 签名。
- 设置页说明补充 Docker 容器内 Emby 的直链地址填写方式。

## v0.1.39 - 2026-08-13

本次是挂载与 Emby 播放链路的整体重构：STRM 全面改为签名直链、移除 Emby 前置代理，WebDAV 读文件改为 302 直链直连 CDN。

### Emby STRM 虚拟库（直链播放）

- STRM 内容从“云端纯路径”改为带 HMAC 签名的播放直链 `http(s)://<直链地址>/strm/<fileId>?sign=…`；新增免管理登录、恒时签名校验的 `/strm/` 播放端点（Docker/Web 在管理端口，桌面端在 18096 直链服务），校验通过后 302 到云盘 CDN。
- Emby 只需把虚拟库这一个目录加入媒体库：不再需要把挂载盘映射给 Emby，客户端照常连接 Emby 原生地址（如 8096），支持 DirectPlay 的客户端直连 CDN 播放。
- 移除 Emby 兼容反向代理（18096 反代与 WebSocket 转发）与 `GUANGYA_EMBY_*` 环境变量；桌面端 18096 端口位改为轻量 STRM 直链服务，旧配置的 `proxy_port` 自动迁移。虚拟库设置以 `strm_base_url`（STRM 直链地址）取代“Emby 原始地址”，Docker 可用 `GUANGYA_STRM_BASE_URL` 首次初始化；修改地址后下次同步自动重写全部 STRM。
- 签名密钥按实例持久化到 SQLite，不通过状态接口回显。

### 挂载性能

- WebDAV 读文件默认 302 重定向到云盘签名直链（`GUANGYA_WEBDAV_REDIRECT=auto`）：rclone、Infuse 等客户端直连 CDN，数据不再经过本进程双跳中转；对 Windows WebClient、macOS Finder、davfs2 等已知不支持重定向的客户端自动回退中转，`off` 可强制全部中转。
- 新增单文件直链缓存（有效期从 URL 签名解析并预留安全余量，回退 30 分钟、上限 60 分钟，并发请求同一文件只调用一次云端接口）：此前每个 Range/HEAD 请求都会调用一次 `get_res_download_url`，rclone 按 4MB 分块顺序读时每 4MB 触发一次云端接口是挂载卡顿的主因。WebDAV 读取、STRM 端点与虚拟库元数据下载共用该缓存，中转路径直链过期（403/410）自动刷新一次重试。
- HEAD 请求直接用目录条目元数据回应，不再触发云端接口与 CDN 请求；桌面端 WebDAV 读取改用全局共享连接池（此前每次读取新建 reqwest 客户端）。
- rclone 托管挂载默认参数调优：读取分块 4M→8M、缓冲 4M→16M、预读 16M→32M、目录缓存 2s→10s。

## v0.1.38 - 2026-08-12

- GCID/CID 导入遇到远程目录或文件“名称不可用”时，先使用 `gy_<随机值>` 临时名称完成创建/秒传，入库后再恢复原名。
- 原名恢复失败时保留云端文件 ID 和临时名，后续仅重试改名，避免重复秒传；名称规则错误不再原样重试 5 次。

## v0.1.37 - 2026-08-12

- GCID/CID 导入兼容 PikPak 的 `source: "pikpak"` 与 `containsCid: true` 导出格式。
- 保留路径、文件大小、GCID/CID 格式校验，并同步 Tauri、CLI、界面提示和文档。

## v0.1.36 - 2026-08-12

本版本是一次大规模的架构整理与缺陷修复：Rust 后端完成模块化拆分，接口层新增统一重试与令牌自动续期，目录缓存/刷新、媒体刮削整理修复了一批高危问题，前端界面全面统一。

### 后端架构

- Rust 后端从单个 22,500 行的 `main.rs` 拆分为 29 个职责清晰的模块（文件、分享、上传、下载、回收站、离线、秒传、整理、挂载、缓存、设置等），入口只保留装配逻辑；内联单元测试移至独立文件。

### 接口层（重试与登录态）

- 新增统一 HTTP 层：全局共享连接池（此前账号接口每次请求新建客户端、部分请求无超时），并跟随"设置 → 网络"的全局代理（此前业务接口忽略代理设置）。
- 按端点幂等性自动重试：列表/详情/搜索等只读接口在网络抖动、429、5xx 时指数退避重试并尊重 `Retry-After`；复制/移动/分享等写接口只重试"请求未送达"的失败，避免重复执行。
- 登录态自动续期：任何请求遇到令牌过期（401 / 业务码 110、117、118）会单飞刷新令牌并自动重放一次。此前只有界面操作能触发续期，上传、WebDAV 挂载、整理器等后台任务令牌过期后会持续失败最长 20 分钟。
- Web 端 `apiPost` 补齐同样的退避重试；两端的云端任务轮询容忍瞬时失败（此前一次 502 就会把已在云端完成的移动/删除报告为失败）；下载分片重试加入退避。

### 目录缓存与刷新

- 修复挂载端大目录被截断：上游返回字符串型 `total` 时，超过 100 项的目录会在第一页后被误判为"已取完"。
- 界面读取目录不再打断挂载端加载（旧行为会导致 rclone 挂载间歇性 503），完整快照直接覆写挂载缓存。
- 上传路径解析缓存加入 15 秒远端复核：目录被其它设备删除或改名后，不再持续把文件传进失效目录。
- 写操作改为按目录精确失效，取代"任何一次写操作都清空全部缓存"（整理器批量操作时界面持续全量刷新、上传路径解析反复重试报错的根源）。
- 缓存作用域改为按账号绑定：令牌每 20 分钟例行轮换不再清空全部挂载目录缓存。
- 补齐失效盲区："清理缓存"现在同时清挂载端快照；回收站增删/清空会通知已打开的回收站面板自动刷新；离线任务完成后自动刷新目标目录。
- 前端列表刷新修复：批量上传期间列表不再"永不刷新"（失效事件去抖加入最大等待）；在途旧响应不再回写缓存造成脏数据；"刷新"按钮真正绕过缓存；写操作后的刷新不再闪空列表、不再与失效事件重复请求；目录选择器分页不再虚高。

### 媒体刮削整理

- 修复复制模式重复执行：轮询命中已完成任务会重新识别并再次执行——"保留两份"策略下每 15 秒生成一份重复文件，开启自动分享时每轮创建一个新分享。
- 修复任务卡死：识别前置读取失败的任务会永久停在"识别中"，并锁死监控配置的修改与删除（此前只能重启应用恢复）。
- 修复样片/花絮误判：路径中含 "sample"、"trailer" 子串的正常影片（如 The Sampler、Trailer Park Boys）不再被整批丢弃或判成预告片。
- 修复季集解析：确定媒体类型后按剧集规则完整重新解析，`Show - 05.mkv` 这类仅含集号的文件不再报"缺少集号"。
- 目标目录解析加入缓存：一个 24 集季的预览从 300+ 次云端列目录降到个位数，显著降低限流与卡顿。
- 其它修复：`{season:02}` 缺季时不再渲染成 "Season 00"；字幕语言后缀输出 Emby/Jellyfin 可识别的 BCP-47 标记并保留 forced/sdh；季海报不再回落到剧集根目录与主海报撞名；技术信息探测改为 4 路并发（此前串行探测最坏可卡 18 分钟）；移动模式的固定提醒不再把所有任务标成黄色警告；执行前检测到源或配置变化会自动重新识别；修改监控配置不再清空待人工复核的任务；轮询失败会记录到监控的错误状态。

### 界面

- 删除约 2,600 行遗留死代码（旧版应用外壳、旧主题、两个未挂载的页面），全局样式表清理约 60% 的失效规则。
- 建立完整的设计 token 体系（间距、字号、圆角、控件高度、层级、页宽），统一三代配色残留。
- 默认控件高度从 22px 提升到 28px，缓解桌面端过于拥挤的问题；表格等高密度场景保持紧凑。
- 顶栏显示当前页面名称；备份、传输、离线、分享、整理五个页面新增统一的标题与说明区。
- 修复备份任务卡片缺失的整组布局样式、空状态样式、长文件名溢出等 20 余处"引用了但从未定义"的样式。
- 设置页 12 个面板的宽度、留白与标题排版统一；整理页的超大标题与蓝绿杂色收敛到全局中性主题。
